# Secrets and runtime auth — never commit
.env
*.env
!.env.example

# Runtime logs + persisted switch state (mounted volume)
logs/
*.log
switch_state.json

# Security reports — local-only, never commit (may detail live vulns/secrets)
SECURITY_REPORT.md

# Python
__pycache__/
*.pyc
